
"use client";

// Este layout ahora es muy simple, ya que DashboardLayout maneja la estructura principal
// y el ActiveCompanyProvider.
// Se usa para la página de "Gestionar Empresas".

export default function EmpresasGestionDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
